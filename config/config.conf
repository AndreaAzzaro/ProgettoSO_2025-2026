# Configurazione Mensa Studentesca 2025-2026
# Le righe con # sono commenti

# --- QUANTITÀ ---
N_WORKERS = 10
N_USERS = 20
N_NEW_USERS = 5
N_PAUSE = 3

# --- POSTI (SEMAFORI) ---
SEATS_PRIMI = 5
SEATS_SECONDI = 5
SEATS_COFFEE = 10
SEATS_CASSA = 2
TOTAL_SEATS = 100

# --- PREZZI (Interi) ---
PRICE_PRIMI = 5
PRICE_SECONDI = 8
PRICE_COFFEE = 1

# --- TEMPI (Simulazione) ---
# Durata in secondi reali
SIM_DURATION = 30
# Moltiplicatore per convertire secondi simulati in nanosecondi reali
# Es: 1 sec sim = 1000000 ns reali (velocizza la sim)
N_NANO_SECS = 1000000

# Tempi medi di servizio (unità simulate)
AVG_SERVICE_PRIMI = 3
AVG_SERVICE_MAIN = 4
AVG_SERVICE_COFFEE = 1
AVG_SERVICE_CASSA = 2
AVG_REFILL_TIME = 10
STOP_DURATION = 5

# --- SOGLIE ---
OVERLOAD_THRESHOLD = 15